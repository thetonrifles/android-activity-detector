language: android
jdk: oraclejdk7

android:
  components:
    - platform-tools
    - tools

notifications:
  email: false

before_install:
  - chmod +x gradlew
  - export TERM=dumb

script: ./gradlew clean build
